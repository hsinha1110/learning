import Routes from './constant/Routes';

export interface ProductInfo {
  id: number;
  title: string;
  price: number;
  description: string;
  images: string[];
  image: string;
  thumbnail: string;
}

